import { X } from "lucide-react";
import React from "react";

const Icons = () => {
  return (
    <div className="text-red-600 z-20 text-left w-full absolute flex-flex-row top-[-40]">
      <X size={70} strokeWidth={4}  />
    </div>
  );
};

export default Icons;
